steps:
  - label: "Build Go App"
    key: "build"
    commands: 
      - cd hello
      - go build
      - block: "Are we ready to deploy?"
      - chmod +x ./hello
      - ./hello buildkite ftw
    plugins:
      - docker#v5.11.0:
          image: "golang:latest"


