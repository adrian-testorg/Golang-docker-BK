steps:
  - label: "Build Go App"
    key: "build"
    commands: 

    plugins:
      - docker#v5.11.0:
          image: "golang:latest"
          volumes:
            - "./hello:/app"  

  - label: "Run Go App"
    depends_on: "build"
    commands: 
      - cd hello
      - go build
      - chmod +x hello/hello
      - ./hello/hello buildkite ftw
    plugins:
      - docker#v5.11.0:
          image: "golang:latest"
          volumes:
            - "./hello:/app" 
